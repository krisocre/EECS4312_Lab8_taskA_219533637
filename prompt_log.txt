## Student Name: MHD-Oubai Al-khimi
## Student ID: 219 533 637

Prompt 1: "System Description: I am building a Python-based Appointment Slot and Medication Reminder Recommender. The system must manage event registrations, waitlists, and medication reminders.

Personas:

Jane (Primary): A busy professional who wants "hands-off" automation, minimal notifications, and automatic conflict resolution.

Mo (Edge): An accessibility-focused user who needs explicit explanations for system changes and absolute consistency to build trust.

Constraints:

C1: Deterministic ordering for identical timestamps.
C2: Mandatory logging/explanation for state changes (e.g., waitlist promotion).
C3: Notification suppression for duplicates within 5 minutes.
C4: Explicit error messages for failures (no silent failures).
C6: System must be idempotent (duplicate requests lead to same state).
C7: Automatic priority-based resolution for overlapping reminders.

Acceptance Criteria (AC):

AC1: Tie-breaking uses alphabetical sorting.
AC2: Waitlist promotions generate a reason-log.
AC3: Identical alerts within 5 mins are filtered.
AC4: Capacity = 0 returns an error status.
AC5: Duplicate registration requests return original confirmation.
AC6: High-priority meds automatically bump low-priority ones.

Task: Please review this specification. Identify any missing constraints, ambiguous requirements, or unhandled edge cases—specifically regarding the conflicting needs of Jane (minimalism) and Mo (transparency). Do not generate code yet."





Response summary: The LLM validated the core constraints but identified a "conflict of interest" between Jane and Mo regarding notification verbosity. It also noted an ambiguity in C7: the system doesn't specify what happens if two tasks have the same high-priority level. It suggested adding a "Notification Level" setting and a secondary tie-breaker for priority conflicts.

Decision: Modify. I will accept the suggestion to add a secondary tie-breaker for priority (TBR4) and add a "Revision Note" to address the notification verbosity conflict.


Prompt 2: "Revisions based on your feedback:
I have updated the requirements to resolve the issues you identified:

Priority Tie-Breaker (Update to C7): If two tasks have the same priority level and same timestamp, the system will default to alphabetical sorting (TBR1).

Notification Level (New Constraint C8): To balance Jane’s need for minimal noise and Mo’s need for clarity, the system will implement a "Silent Log" for routine automated changes (like waitlist promotions) and only trigger "Urgent Alerts" for medication reminders.

Timezone Handling (New Assumption): For this lab, we assume all inputs are in the local system time (EST) to avoid offset ambiguity.

Task: Confirm if these revisions address the ambiguities. If the specification is now complete and unambiguous, let me know so we can move to implementation."

Response summary: The LLM agreed that the additions (C8 and the priority tie-breaker) successfully bridged the gap between the two personas. It confirmed the specification is now unambiguous and ready for implementation.

Decision: Accept. The requirements are finalized. I am moving to the implementation phase.


